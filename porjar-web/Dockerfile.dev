FROM node:22-alpine

WORKDIR /app

# Cache dependencies
COPY package.json package-lock.json ./
RUN npm ci

# Copy source (volumes will override in dev)
COPY . .

# Next.js dev server on custom port
ENV PORT=4200
EXPOSE 4200

CMD ["npm", "run", "dev", "--", "-p", "4200"]
